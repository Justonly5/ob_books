DSH 的插件几乎都是 **同一个 npm 包、两个半区**：
- **host 半区（node 端）**：`src/index.ts` → 编译成 `lib/index.js`。在服务器/Node 侧运行，负责逻辑、服务、配置。
- **client 半区（浏览器端）**：`src/client/index.ts` → 编译成 `lib/client.js`。在浏览器里运行，负责 UI、slot 渲染。